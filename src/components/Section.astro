---
const { title, eyebrow, id, class: className = "" } = Astro.props;
---

<section id={id} class={`section-stack ${className}`}>
  <div class="section-heading">
    {eyebrow && <p>{eyebrow}</p>}
    <h2>{title}</h2>
  </div>
  <slot />
</section>
