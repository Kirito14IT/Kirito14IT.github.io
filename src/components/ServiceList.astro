---
import type { ServiceItem } from "@data/cv";
import { getUi, type Locale } from "@i18n/index";

const { items, locale } = Astro.props as { items: ServiceItem[]; locale: Locale };
const copy = getUi(locale);
---

<div class="service-list">
  {items.map((item) => (
    <article class="service-card">
      <div class="service-meta">
        <time>{item.year}</time>
        <span>{item.category}</span>
      </div>
      <div class="service-copy">
        <h3>{item.role}</h3>
        <p>{item.description}</p>
      </div>
      <span class="service-watermark" aria-hidden="true">{copy.serviceWatermark}</span>
    </article>
  ))}
</div>
